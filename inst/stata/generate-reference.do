* Golden-reference harness for Stata jwdid.
* Populate expected-results.csv on a machine with Stata and jwdid installed.
clear all
display "Run the mpdta option grid and export estimates to expected-results.csv"
