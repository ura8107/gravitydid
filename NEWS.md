# jwdid 0.0.1

- Initial package implementation.
- The primary aggregation API is `jwdid_aggte()` because `did` exports an
  unrelated, non-generic function named `aggte()`. Use `estat()` or the
  `jwdid_*()` convenience wrappers as alternatives.
