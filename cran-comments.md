## Test environments

* local macOS, R release
* GitHub Actions: macOS, Windows, and Ubuntu (R release)
* GitHub Actions: Ubuntu (R devel and oldrel-1)

## R CMD check results

0 errors | 0 warnings | 0 notes

## Notes

This is a new release.

Examples and vignettes use the small public `mpdta` data set from the suggested
`did` package. Network access is not required during package checks.
